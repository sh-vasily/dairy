services:
  api:
    build:
      context: .
      dockerfile: Dairy.Api/Dockerfile
    restart: always
    volumes:
      - c:\ProgramData\Dairy\:/var/data
  ui:
    build: Dairy.UserInterface
    ports:
      - 80:80